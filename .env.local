# Leave VITE_API_URL empty to use the same origin as the frontend (correct for production).
# Set this only if your backend is hosted on a different domain, e.g.:
# VITE_API_URL=https://my-backend.onrender.com
VITE_API_URL=
